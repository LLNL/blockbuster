
#include "BufferedStreamingMovie.h"

using namespace boost; 



bool BufferedStreamingMovie::DisplayFrame(uint32_t framenum){
  // update the current image: 
  
  // if the front buffer has the image, then display it. 

  // else, 
  return false; 
}
